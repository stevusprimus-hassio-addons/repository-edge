---
name: Stale

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  workflows:
    # yamllint disable-line rule:line-length
    uses: stevusprimus-hassio-addons/workflows/.github/workflows/stale.yaml@main
