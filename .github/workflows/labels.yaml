---
name: Sync labels

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "34 5 * * *"
  workflow_dispatch:

jobs:
  workflows:
    # yamllint disable-line rule:line-length
    uses: stevusprimus-hassio-addons/workflows/.github/workflows/labels.yaml@main
